<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black"
    tools:context=".CreateNoteActivity">


    <EditText
        android:id="@+id/edt_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginTop="20dp"
        android:background="@null"
        android:singleLine="true"
        android:hint="Title"
        android:textStyle="bold"
        android:textColor="@color/white"
        android:textColorHint="@color/grey"
        android:textSize="28dp"
        android:layout_marginHorizontal="12dp"/>

    <ImageView
        android:id="@+id/img_share"
        android:layout_width="54dp"
        android:layout_height="42dp"
        app:layout_constraintTop_toTopOf="@id/img_save_create_note"
        app:layout_constraintBottom_toBottomOf="@id/img_save_create_note"
        app:layout_constraintEnd_toStartOf="@id/img_save_create_note"
        android:layout_marginEnd="16dp"
        android:src="@drawable/share"/>

    <View
        android:id="@+id/view"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/grey"
        app:layout_constraintTop_toBottomOf="@id/edt_title"
        android:layout_marginTop="9dp"/>



    <EditText
        android:id="@+id/edt_note"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/view"
        app:layout_constraintBottom_toBottomOf="parent"
        android:background="@null"
        android:hint="Notes"
        android:gravity="top"
        android:textSize="24dp"
        android:textColorHint="@color/grey"
        android:textColor="@color/white"
        android:layout_marginTop="19dp"
        android:layout_marginHorizontal="12dp"/>


    <TextView
        android:id="@+id/date_create_note"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:textSize="16dp"
        android:layout_margin="9dp"
        android:textColor="@color/grey"
        android:text=""/>

    <ImageView
        android:id="@+id/img_save_create_note"
        android:layout_width="32dp"
        android:layout_height="32dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:src="@drawable/done"
        android:layout_marginTop="9dp"
        android:layout_marginEnd="9dp"/>


</androidx.constraintlayout.widget.ConstraintLayout>